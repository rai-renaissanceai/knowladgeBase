# In production (e.g. Vercel), point the static frontend at the separately-hosted
# backend (Express + SQLite on Render/Railway/Fly.io/a VM with persistent disk).
# Leave unset for local dev — the Vite dev server proxies /api and /minutas instead.
VITE_API_URL=https://your-backend-host.example.com
